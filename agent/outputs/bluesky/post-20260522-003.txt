Most "AI agents" aren't agents. They're scripts with a retry loop.

Real agents: persistent memory, self-revision, goal tracking with velocity.

The gap is measurement. Agents that can't measure their own output can't improve. They just repeat.
