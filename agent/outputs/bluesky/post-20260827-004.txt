Most "AI agents" are API wrappers with a loop — stateless, no limits, no memory.

A real agent maintains state, knows when to stop, and creates durable output that compounds over time.

That gap is the whole problem this template addresses.